# Modbus Workbench 使用说明

跨平台（**macOS 优先，兼顾 Windows / Linux**）的高性能 Modbus 调试与测试工作台。采用 Tauri 2.x + Rust 核心架构，界面响应迅速、占用空间低（二进制仅 ~3.4MB），且内置全功能模拟器和动态 PDU 报文追踪功能。

---

## 目录
1. [技术架构](#1-技术架构)
2. [主工作台功能使用](#2-主工作台功能使用)
   - [2.1 通道连接配置](#21-通道连接配置)
   - [2.2 轮询与读写配置](#22-轮询与读写配置)
   - [2.3 底层报文追踪 (Trace Panel)](#23-底层报文追踪-trace-panel)
   - [2.4 寄存器解析表](#24-寄存器解析表)
   - [2.5 Unit ID 扫描（从站发现）](#25-unit-id-扫描从站发现)
3. [内置从站模拟器 (Simulator)](#3-内置从站模拟器-simulator)
   - [3.1 通道参数设置](#31-通道参数设置)
   - [3.2 寄存器增删改与批量添加](#32-寄存器增删改与批量添加)
   - [3.3 自动变化机制](#33-自动变化机制)
   - [3.4 Excel 导入与导出](#34-excel-导入与导出)
   - [3.5 缺省地址回应 0xFFFF](#35-缺省地址回应-0xffff)
4. [运行与开发编译](#4-运行与开发编译)

---

## 1. 技术架构

- **后端内核 (`crates/modbus-core`)**：纯 Rust 开发的协议栈，包含独立的 TCP/UDP/RTU 帧编解码器、寄存器转换模块、主站客户端、多 Unit 镜像从站和事件广播机制。
- **通信桥接 (`src-tauri`)**：使用 Tauri 2.x 调度，将原生的串口 (`serial2`) 与网络通信绑定到 Rust 线程。
- **前端展示 (`src/`)**：基于 Vite + TypeScript + Vanilla CSS 构建，极速渲染，支持 macOS 磨砂毛玻璃毛皮原生视觉体验。

---

## 2. 主工作台功能使用

### 2.1 通道连接配置
点击主界面左上角 **⬤ 连接** 按钮可打开通道连接配置：
- **Modbus TCP / Modbus UDP**：填写目标主机的 IP 地址与端口号。
- **RTU over TCP / RTU over UDP**：使用 TCP/UDP 传输无 MBAP 报头包裹的原始 RTU 帧，用于对接串口服务器或网关。
- **Modbus RTU (串口)**：选择本机串口名（如 `/dev/cu.usbserial-*`），支持自定义波特率、数据位、停止位和校验方式。

### 2.2 轮询与读写配置
主工作台通过“轮询配置 (Poll Configuration)”统一管理读写：
- **标签页切换**：支持点击 `+ 添加轮询` 独立分配不同的起始地址与操作，标签页在后台并发轮询。
- **操作 Function Code**：
  - **读操作** (`01 读线圈`、`02 读离散输入`、`03 读保持`、`04 读输入`)：可选择开启 **☑ 定时轮询** 复选框（并指定毫秒周期），或点击 **发送** 按钮手动立即读取单次数据。
  - **写操作** (`05 写单线圈`、`06 写单寄存器`、`15 写多线圈`、`16 写多寄存器`)：周期轮询复选框将自动隐藏，显示 **写入值** 输入框。
- **多写智能数量计算**：在 `15` 或 `16` 写入逗号分隔序列（如 `1,0,1` 或 `58770,3,4`）时，**数量字段会自动计算并填充为数值的实际长度**，无需手动修改。

### 2.3 底层报文追踪 (Trace Panel)
在轮询配置下方，提供了**底层报文调试条**，在单次发送或定时轮询时，实时抓包呈现底层字节流：
- **发送 PDU / 发送 ADU**：展示主站请求 the PDU 部分及完整的 ADU（RTU 含校验，TCP 含 MBAP）。
- **响应 PDU / 响应 ADU**：展示从站回应的 PDU 以及完整的 ADU 字节。
- **耗时**：显示往返延迟 (RTT) 毫秒数。

### 2.4 寄存器解析表
展示已读取的寄存器数据。每行均可独立切换：
- **显示格式**：`U16` / `I16` / `U32` / `I32` / `F32`。
- **字序转换**：`ABCD` (大端序) / `CDAB` (双字节交换) / `BADC` / `DCBA` (小端序)。
- 支持直接修改某一行并点击该行的 **写入** 快速同步更新数据。

### 2.5 Unit ID 扫描（从站发现）

在不清楚总线上挂了哪些从站、或哪些 Unit ID 在监听时，可使用 **Unit ID 扫描** 功能快速探测。点击工具栏中的 **⌕ 扫描**（需先成功建立通道连接，连接后按钮才激活；断开会禁用并回收扫描窗口）即可打开独立的扫描窗口。

扫描窗口基于主站工作台已有的 `read_points`（FC03 读保持寄存器）逻辑，逐 Unit ID 发起读取请求：

1. **指定寄存器地址**：在「读取寄存器地址」输入框填写一个保持寄存器地址（默认 `0x100`，支持十六进制 `0x` 前缀或十进制），作为探测每个 Unit ID 时读取的寄存器。
2. **扫描范围**：自动依次探测 **Unit ID 1 ~ 247**，每个 ID 发起一次读请求。
3. **响应可视化**：对某 Unit ID 收到有效响应的，页面以**绿色圆形色块**展示，色块中心显示对应的 **Unit ID 数值**；未响应（超时、异常响应或 Modbus 异常）的 Unit ID 不显示任何色块。
4. **进度计数**：
   - 「开始扫描」按钮**前方**实时显示 **已扫描 XX 个**（已探测的 Unit ID 总数，含无响应）。
   - 页面头部显示 **X 个响应**（实际产生绿色色块的响应总数）。
5. **开始 / 停止**：
   - 点击 **▶ 开始扫描** 启动探测。
   - 点击 **■ 停止扫描** 可在扫描过程中随时中止（在当前迭代间隙即时停止，并更新状态为“已停止”）。
6. **窗口尺寸**：扫描窗口默认高度经过加高处理，可完整容纳最多约 40 个响应色块而不必滚动，绿色圆球已适度缩小以保证紧凑排布。

> 说明：扫描为顺序逐 ID 探测，超时（默认 1000ms）模式下最差约 247 × 超时时长。若总线响应慢，可随时点「停止扫描」中断；该过程仅读取、不会对被探测从站写入。

---

## 3. 内置从站模拟器 (Simulator)

点击主界面工具栏中的 **⚙ 模拟器** 即可拉起一个与主页面大小相同的独立模拟器窗口。

### 3.1 通道参数设置
- 新窗口右上角为 **⚙ 通道设置**。默认支持 TCP、UDP、RTU-over-TCP、RTU-over-UDP 及真实串口 RTU 等 5 种物理连接的同时运行与启停。
- 模拟器若在启动时未检测到任何已连接通道，将自动弹出通道设置弹窗，引导您快速上线。

### 3.2 寄存器增删改与批量添加
- 在“新增寄存器”弹窗中，不仅可以配置点位名称、区域与数据类型，还可以指定 **数量** 以实现**一次性连续增加**寄存器。
- 支持地址排冲突检验，自动避开已存在的地址。

### 3.3 自动变化机制
每个寄存器支持独立的 Vary 机制：
- **Off**：保持当前数值。
- **正弦 (Sine)**：数值按照正弦波形在 `0 ~ 65535` 范围内周期变化。
- **随机 (Random)**：数值随机在 `0 ~ 65535` 变化。
- **自增 (Increment)**：每次节拍加 1。

### 3.4 Excel 导入与导出
- **📂 导入 Excel** / **💾 导出 Excel**：将当前单元 (Unit) 下的所有寄存器配置（区域、地址、名称、类型、访问模式、变化方式及值）保存为标准的 `.xlsx` 文件，亦可通过 Excel 修改属性后，增量导入并排重合并。

### 3.5 缺省地址回应 0xFFFF
- 为更贴近真实的物理设备表现，当主站读取模拟器中**未显式添加在寄存器列表里**的空闲寄存器地址时，模拟器不再返回 `0x0000`，而是统一高低字节回应为 **`0xFFFF`**（无数据状态码），同时保证测试不报错。

---

## 4. 运行与开发编译

### 安装与运行：
```bash
# 1. 安装前端 Node 依赖
npm install

# 2. 以开发模式启动（会自动启动 Vite 前端 + Rust Tauri 外壳）
npm run tauri dev
```

### 构建 Release 独立安装包：
```bash
# 自动编译前端 dist 并调用 Rust LTO 跨 crate 全局优化打包，减小二进制大小
npm run tauri build
```
编译产物位于 `src-tauri/target/release/` 下，其中包含极度压缩优化后的高性能客户端。
